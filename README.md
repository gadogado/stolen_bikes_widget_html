### Setup

* `npm install`
* `npm start`
*  Visit [localhost](http://localhost:8080/)

### Initialization

```
// within a <script> tag:

BikeIndex.init({
  location: 'Portland, OR'
});
```

OR with default location of `''`

```
BikeIndex.init();
```