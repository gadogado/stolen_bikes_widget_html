### Setup

* `npm install`
* `npm start`
*  Visit [localhost](http://localhost:8080/)

### Initialization [WIP]

```
// within a <script> tag:

BikeIndex.init({
  location: 'Portland, OR'
});
```

OR with defaults `''`

```
BikeIndex.init();
```